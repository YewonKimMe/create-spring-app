spring:
  application:
    name: create-spring-app
  docker:
    compose:
      enabled: true
  config:
    import: optional:file:create-spring-app.env[.properties]
  profiles:
    active: ${SPRING_PROFILES_ACTIVE} # 배포 시  env 파일에서 prod 로 변경